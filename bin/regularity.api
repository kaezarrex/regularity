#! /usr/bin/env python

if '__main__' == __name__:

    import web

    from regularity.web import api

    urls = (
        ('/client/create', api.ClientAPI)
    )

    app = web.application(urls, {})
    app.run()

